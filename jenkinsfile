pipeline {
    agent { 
    dockerfile { 
   label 'slave'
   additionalBuildArgs  '--build-arg version=1.0.2'
   } 
   }
    stages {
        stage('build') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
  
