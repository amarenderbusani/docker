pipeline {
  agent {
    dockerfile {
        label 'slave'
        additionalBuildArgs  '--build-arg version=1.0.2'
        args '-v /tmp:/tmp'
    }
  }
  stages {
  stage('Notify') {
  steps {
 sh "hostname"
 }
      }
      
  }
    }
