pipeline {
    agent { 
    dockerfile { 
   label 'my-defined-label'
   additionalBuildArgs  '--build-arg version=1.0.2'
   } 
   }
    stages {
        stage('build') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
  
